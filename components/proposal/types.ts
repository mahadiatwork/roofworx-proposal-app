// Re-export shared types from mock-data for component use.
// When a real API is wired up, these can be replaced with generated API types.
export type { LineItem, ProposalSection, Proposal, JobMeta, CatalogItem, ExistingProposalSummary } from "@/lib/mock-data";
